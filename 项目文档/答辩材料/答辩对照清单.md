# OJ 调试平台 — 答辩对照清单（v1.4.1）

> 官方文档锚点：`https://dbg-course.github.io/python-docs/oj/`
> 总满分 = 功能 40（Step1~6 各 5 分） + 规范 5 分 + 报告 5 分 = 50 分；Advance AI 额外 10 分；有扣分项。

---

## 总览：前置硬约束（不满足整份 0 分）

| 官方要求 | 本项目对应位置 | 演示建议 |
| --- | --- | --- |
| **使用 FastAPI 的异步编程接口（所有路由 `async def`）** | `webapp/app_fastapi.py` 所有业务路由（`api_auth_* / api_problems_* / api_submissions_* / api_*_lang` 等）均标记 `async def`，L89 用 uvicorn ASGI 启动 | 打开 `http://127.0.0.1:5000/docs` 看 Swagger，随机挑几条路由看代码签名即可 |
| **所有前端页面（Streamlit）不得直连 DB，** **必须走 API** | 所有 `webapp/pages/*.py` 均用 `common.api()` 封装 `requests.Session` 调 `http://127.0.0.1:5000/api/...`；项目中未发现任何前端 `import sqlite3` 或 `import userdb` | 在个人信息页/题目页随便点一个功能，看 FastAPI 控制台的请求日志即可 |

---

## Step 1：题目管理（5 分，官方 Step1：`/project/step1/`）

| 官方要求 | 分值 | 后端路由 / API | 前端页面 | 对应源码文件 | 演示建议 |
| --- | --- | --- | --- | --- | --- |
| 题目列表（分页 + 难度/标签筛选） | 3 / 3 列表部分 | `GET /api/problems?page=&page_size=&difficulty=&tag=` 响应头带 `X-Total-Count / X-Page / X-Page-Size` | `🗂_题目管理.py`（题库浏览） | `webapp/app_fastapi.py::api_problems_list`；`webapp/pages/🗂_题目管理.py` | 普通用户登录后点左侧「题目管理」，翻页 / 按难度下拉筛选 |
| 题目详情（展示完整 JSON 字段 8 必选 7 可选） | 3 / 3 详情部分 | `GET /api/problems/{pid}` | `📄_题目详情.py` | `webapp/app_fastapi.py::api_problems_detail`；`webapp/userdb.py::get_problem` | 题目列表点「详情」，检查 title / description / input_spec / output_spec / sample_in / sample_out / time_limit / memory_limit 八项必选字段齐全 |
| 新增题目（管理员，校验 JSON 字段） | 2 / 2 增改部分 | `POST /api/problems`（Body 校验 fields、time_limit >0、memory_limit>0） | `🗂_题目管理.py`（管理员 Tab：新增题目） | `webapp/app_fastapi.py::api_problems_create` | admin 登录，切「题目管理 → 新增题目」，填 A+B 题，点提交后列表立即出现 |
| 编辑题目（管理员） | 2 / 2 增改部分 | `PUT /api/problems/{pid}` | `📄_题目详情.py`（管理员：编辑按钮） | `webapp/app_fastapi.py::api_problems_update` | 进入某题目详情页，点「编辑」改标题，刷新后生效 |
| 删除题目（管理员） | 2 / 2 增改部分 | `DELETE /api/problems/{pid}`（404 / 403 异常分级） | `📄_题目详情.py`（管理员：删除按钮 + 二次确认） | `webapp/app_fastapi.py::api_problems_delete` | 新建测试题 → 删除 → 列表不再出现 |

---

## Step 2：题目评测（5 分，官方 Step2：`/project/step2/`）

| 官方要求 | 分值 | 后端路由 / API | 前端页面 | 对应源码文件 | 演示建议 |
| --- | --- | --- | --- | --- | --- |
| Python 语言支持（基础评测） | 2 / 2 多语言部分 | `POST /api/submissions`（language='python'） → `oj_judge.Judger.judge()` | `⚖️_判题器.py?id=xxx` 页面 | `oj_judge/judger.py`、`oj_judge/executor.py`、`webapp/app_fastapi.py::api_submissions_create` | 打开 A+B 题判题器，选 Python，填 `print(sum(map(int,input().split())))` 提交，判 AC |
| C++ 语言支持（g++ 编译 + 执行） | 2 / 2 多语言部分 | `POST /api/submissions`（language='cpp'）→ `Language.CPP.compile_cmd` 调 g++ | `⚖️_判题器.py` 下拉 language 选 C++ | `webapp/userdb.py::find_cpp_compiler`（自动查 msys2 路径）、`oj_judge/constants.py::Language` | 同 A+B 用 C++ `int main(){...}` 提交，判 AC；若未装 g++ 会在「编译错误」段有清晰提示 |
| **动态注册新语言**（助教最常查） | 1 分 | `POST /api/languages`（Body: `{name, compile_cmd, run_cmd, file_ext, time_multiplier}`） + `GET /api/languages` + `DELETE /api/languages/{name}` | `🔣_语言管理.py`（管理员页） | `webapp/app_fastapi.py::api_create_language / api_list_languages / api_delete_language`；`webapp/userdb.py::register_language` | **答辩关键演示**：管理员登录 → 打开「🔣 语言管理」→ 新增 Go 语言（name='go', compile_cmd='go build -o {bin} {src}', run_cmd='{bin}', file_ext='.go'）→ 再打开判题器，语言下拉立即出现 Go 选项 |
| 查询语言列表（含动态注册的） | 1 分 | `GET /api/languages` 返回所有 `(name, enabled, is_builtin)` | `🔣_语言管理.py`（表格展示） | 同上 | 直接在「🔣 语言管理」看表格，列清楚 Python / C++ / Java 是 builtin，新注册的 is_builtin=false |
| TLE / MLE 超时超内存识别与报错 | 1 分 | `Judger.judge()` 内 `subprocess.run(..., timeout=case.time_limit*lm.time_multiplier)`；超出 → `JudgeStatus.TLE`；超过 output_limit → `JudgeStatus.OLE`；异常退出 → `JudgeStatus.RE` | `🧾_提交详情.py` 的单测试点状态列 | `oj_judge/judger.py::judge()`（try/except subprocess.TimeoutExpired 分支）；`oj_judge/executor.py::execute_program` | 提交一个 `while True: pass` 的 Python 程序，看状态变成 TLE 并带小沙漏图标 |

---

## Step 3：评测列表（5 分，官方 Step3：`/project/step3/`）

| 官方要求 | 分值 | 后端路由 / API | 前端页面 | 对应源码文件 | 演示建议 |
| --- | --- | --- | --- | --- | --- |
| 提交列表 + 分页 + 筛选（by 用户 / by 题目 / by 状态） | 2 分 | `GET /api/submissions?page=&page_size=&user_id=&problem_id=&status=` → 响应头 `X-Total-Count` 等 | `📋_提交日志.py`（顶部三个下拉筛选器 + 分页器） | `webapp/app_fastapi.py::api_submissions_list`；`webapp/pages/📋_提交日志.py` | 打开「📋 提交日志」：按状态筛选 WA、按题目筛选 A+B，每页条数切换 10/20 |
| 单条提交详情（权限校验：本人/管理员可见） | 2 分 | `GET /api/submissions/{sid}` + 401/403 分级 | `🧾_提交详情.py`（含代码、总结果、测试点展开） | `webapp/app_fastapi.py::api_submissions_detail` 中的 `_ensure_viewable` 守卫 | 用普通用户 123321 看别人的 submission → 返回 403；管理员登录同一条 → 正常打开 |
| **管理员重判**（助教常查） | 1 分 | `POST /api/submissions/{sid}/rejudge`（需 admin，401→403→404→200 顺序） | `📋_提交日志.py` / `🧾_提交详情.py` 管理员按钮「🔄 重判」 | `webapp/app_fastapi.py::api_submissions_rejudge`（重跑 `Judger.judge()` 并 UPDATE 覆盖原记录） | 管理员登录 → 提交日志找某条 AC → 点「重判」→ 状态立即刷新为 RUNNING→AC/WA |

---

## Step 4：用户管理（5 分，官方 Step4：`/project/step4/`）

| 官方要求 | 分值 | 后端路由 / API | 前端页面 | 对应源码文件 | 演示建议 |
| --- | --- | --- | --- | --- | --- |
| 初始 admin 账号（任务 0） | 隐含 | `userdb.py::ensure_initial_admin()` 启动时插入 `admin / admintestpassword` | 登录页即可登录 | `webapp/userdb.py::ensure_initial_admin` + 注册接口拒绝重复注册 admin | 首次启动后直接用 `admin / admintestpassword` 登录即可 |
| 用户注册（密码 = password2 校验 + 哈希） | 2 分 | `POST /api/auth/register`（username 正则、长度、密码不一致 400、用户名冲突 409） | `🔐_登录注册.py` Tab2「注册」 | `webapp/app_fastapi.py::api_auth_register`；`webapp/userdb.py::create_user`（bcrypt 优先、hmac 降级） | 访问首页会跳转登录页 → 切注册 Tab → 注册 `testuser / testpass`，409 / 400 边界都能触发 |
| 用户信息查询（v1.4.1 实时统计同步） | 1 分 | `GET /api/auth/me` / `GET /api/user/{id\|me}` / `GET /api/users/{id}` → 返回前调 `recompute_user_stats(uid)` 实查 submissions 表并回写 users.submit_count / resolve_count | `👤_个人信息.py` 顶部 6 个 metric | `webapp/app_fastapi.py::api_auth_me / api_user_info`；`webapp/userdb.py::recompute_user_stats` | 演示关键：打开「👤 个人信息」查看 submit_count / resolve_count → 去判题器提交 1 次 → 回个人信息页刷新，数字 +1 |
| 用户权限变更（管理员改角色） | 1 分 | `PUT /api/users/{id}/role`（role ∈ {user, admin, banned}，403/400/404） | `👥_用户管理.py`（管理员：下拉改角色 + 立即生效） | `webapp/app_fastapi.py::api_users_change_role`；`webapp/userdb.py::update_user_role` | admin 登录 → 「👥 用户管理」→ 把某 testuser 改成 banned → 该用户刷新页面自动登出并提示「账号已禁用」 |
| 用户列表（管理员分页 + 按用户名搜索） | 1 分 | `GET /api/users?page=&page_size=&search=` + 响应头 `X-Total-Count` | `👥_用户管理.py`（搜索框 + 分页器） | `webapp/app_fastapi.py::api_users_list` | 在「👥 用户管理」输入用户名模糊搜索，匹配行高亮 |

---

## Step 5：日志与权限（5 分，官方 Step5：`/project/step5/`）

| 官方要求 | 分值 | 后端路由 / API | 前端页面 | 对应源码文件 | 演示建议 |
| --- | --- | --- | --- | --- | --- |
| **评测日志记录与查询**（按 submission_id 关联） | 2 分 | `GET /api/submissions/{sid}/log` 返回 `{data: {submission_id, visibility, details:[{case_id,status,time_ms,memory_kb,in,out,expected,stderr}]}}`（单数路由，details 是数组） | `🧾_提交详情.py`（「测试点明细」折叠展开） | `webapp/app_fastapi.py::api_submissions_log_detail`（读 submissions 表的 detail_json 字段）；`oj_judge/judger.py` 写入时用 `json.dumps(list(result.details))` | 提交任一代码 → 打开「🧾 提交详情」→ 「测试点明细」Tab，每一条 case 的输入/输出/期望/错误堆栈都能点开看 |
| **日志/测例权限管理**（细粒度） | 2 分 | 权限策略：① `public_cases=False`（默认） → 仅 submission.user_id 本人 + admin 可见详情，其他用户 403；② 管理员 `PUT /api/problems/{pid}/permissions public_cases=true` → 所有登录用户均可见该题目所有 submission 的 case 详情 | `🧾_提交详情.py`（可见性提示 + 测试点展示/隐藏）；`📄_题目详情.py`（管理员「可见性」Tab） | `webapp/app_fastapi.py::api_submissions_log_detail::_ensure_case_viewable` 守卫；`api_problems_update_detailed_perms`（PUT log_visibility） | **关键演示**：用 A 用户提交 → B 用户打开同一条 submission → 403 / 看不到测试点；管理员在「题目详情 → 可见性」把 `public_cases` 打勾 → B 用户再刷新，即可看到所有 case 明细 |
| **审计：access_logs 记录谁访问了哪些 log** | 1 分 | `GET /api/logs/access/` 返回数组（注意：数组 + 响应头 `X-Total-Count / X-Page / X-Page-Size`）；每次访问 `*/submissions/{sid}/log*` 时自动 INSERT 一条 access_logs | `🧾_提交详情.py` 管理员底部「📝 访问审计」表格；或直接 `GET /api/logs/access/` 看 JSON | `webapp/app_fastapi.py::api_logs_access_list`（分页数组）；`api_submissions_log_detail` 开头的 `INSERT INTO access_logs` | 先访问几条 submission 详情 → admin 打开提交详情页底部的「访问审计」Tab / 或直接 `curl http://127.0.0.1:5000/api/logs/access/` → 能看到刚才的访问记录（who,when,ip,submission_id,granted） |

---

## Step 6：前端交互（5 分，官方 Step6：`/project/step6/`）

| 官方要求 | 分值 | 前端页面组（4+5+5=14 页，对应 Streamlit pages/ 14 文件） | 对应源码文件 | 演示建议 |
| --- | --- | --- | --- | --- |
| **用户页面组（注册 / 登录退出 / 用户信息 / 角色管理）** | 2 分 | `🔐_登录注册.py`（登录 Tab + 注册 Tab + 退出按钮）；`👤_个人信息.py`（6 metric + 开始做题 + 退出）；`👥_用户管理.py`（管理员改角色） | 见 `webapp/pages/` 对应三个文件名 + `webapp/common.py` 中的 `refresh_me() / page_url()` | 演示流程：首页自动跳登录 → 注册 testuser → 自动进个人信息（metric 出现）→ 点右上角「👋 退出登录」 → 用 admin 登录 → 进「👥 用户管理」把 testuser 改成 banned |
| **题目页面组（列表 / 详情 / 新增 / 编辑 / 删除）** | 1 分 | `🗂_题目管理.py`（列表浏览 + 管理员 Tab 新增）；`📄_题目详情.py`（详情 + 管理员编辑/删除/可见性） | 见 `webapp/pages/` 对应两个文件名 | 演示：点「🗂 题目管理」浏览列表 → 点一题看详情 → 管理员身份下能看到「编辑/删除/可见性」三个受限按钮 |
| **评测与提交页面组（代码提交 / 提交列表 / 详情 / 状态 / 编译错误信息）** | 1 分 | `⚖️_判题器.py?pid=xxx`（提交代码，展示 RUNNING→AC/WA 状态、编译错误 stderr、总用例通过率）；`📋_提交日志.py`（列表 + 分页 + 筛选 + 重判）；`🧾_提交详情.py`（单条详情 + 测试点） | 见 `webapp/pages/` 对应三个文件名 | 演示：在题目详情点「去判题」→ 判题器自动带 `?id=xxx` → 提交一段故意语法错的代码 → 「编译错误」折叠段立即显示 Python traceback；再提交正确代码 → 状态变 AC |
| **前端与后端接口对接（统一 API 封装 / 会话传递 / 异常处理）** | 1 分 | `webapp/common.py`（`api()` 统一封装三元组 `(status, payload, err)` + `Session` 自动带 Cookie；`refresh_me()` 每次页面渲染检查 `/api/auth/me`；401 自动跳登录、403 友好 `st.error`、429 限流友好提示） | `webapp/common.py::api()` 所有分支；`@require_login` 装饰器；`exception_mapping` | **答辩话术重点**：打开 `common.py` 头 docstring → 讲清楚"所有前端页面都只调 `common.api()`，它统一处理 401/403/429/500 和 `{code,msg,data}` 解包，避免每页重复写" |

---

## Advance：AI 智能命题（额外 10 分，官方 Advance：`/project/advance/`）

| 官方要求 | 分值 | 后端路由 / API | 前端页面 | 对应源码文件 | 演示建议 |
| --- | --- | --- | --- | --- | --- |
| **R1. 出题交互界面**（完整可操作 + 与题目新增/修改合理衔接） | 1 分 | `POST /api/ai/generate` 生成草稿 → `GET /api/ai/drafts` → `POST /api/ai/drafts/{id}/commit` 一键转正式 problem | `🤖_AI命题.py`（独立 AI 命题页：输入知识点/难度/题型 → 生成草稿 → 草稿列表 → 「一键入库」按钮） | `webapp/app_fastapi.py` 中 `api_ai_generate / api_ai_list_drafts / api_ai_commit_draft`；`webapp/pages/🤖_AI命题.py` | admin 登录 → 打开「🤖 AI 命题」→ 填知识点「数组求和」、难度 Easy → 点生成 → 草稿出现题目/样例/测试点 → 点「一键入库」→ 题库管理立即出现新题 |
| **R2. 可自定义模型配置**（provider URL / 模型名 / 密钥；密钥加密不落明文） | 1 分 | `GET /api/ai/config`、`PUT /api/ai/config`：`api_key_encrypted` 字段用 Fernet 对称加密，密钥优先 `OJ_AI_ENC_KEY` 环境变量，否则 `%USERPROFILE%/.trae/oj_ai_key.json` 自动生成 | `⚙️_AI配置.py`（管理员：provider 下拉、base_url、model_name、api_key 密码框、测试按钮） | `webapp/ai_crypto.py::encrypt_api_key / decrypt_api_key / safe_log`（日志自动打码 sk-/gsk- 前缀）；`webapp/app_fastapi.py::api_ai_get_config / api_ai_put_config` | 在「⚙️ AI 配置」填 DeepSeek（https://api.deepseek.com + deepseek-chat + 自己的 key）→ 点「测试连通性」→ 再直接 `SELECT * FROM ai_config` 查，api_key 是乱码 Fernet token，不是明文 |
| **R3. 实时进度渲染 + 中断功能**（流式 SSE / 轮询，不能只在全部完成后返回） | 1 分 | `POST /api/ai/generate` 内部 `async for chunk in Engine.stream_generate():` 写 `ai_task_logs.progress += chunk`；`GET /api/ai/tasks/{uuid}/progress` 轮询；`POST /api/ai/tasks/{uuid}/cancel` 置 cancel_flag=true | `🤖_AI命题.py` 生成期间 `st.progress` 进度条 + chunk 流式追加文本 + 「⏹ 中断生成」按钮，点后立即停止并提示"已取消" | `webapp/ai_engine.py::AIEngine.stream_generate`（支持 cancel_flag）；`webapp/app_fastapi.py::api_ai_task_progress / api_ai_task_cancel` | 开始生成一道较难题 → 进度条在动（非卡死）→ 生成到一半点「中断」→ 立即停止，状态显示 CANCELLED |
| **R4. Token 用量 + 价格统计**（输入/输出分别记录，费用依据说明） | 1 分 | `ai_task_logs(prompt_tokens, completion_tokens, cost_usd, currency)` 表；`GET /api/ai/tasks` 汇总 | `🤖_AI命题.py` 每次生成完成后卡片底部展示："本次调用：输入 N tokens / 输出 M tokens / 费用 $X（模型价格表：deepseek-chat $0.14/1M input $0.28/1M output）" | `webapp/ai_engine.py::_count_tokens`（tiktoken 或近似）+ `_calc_cost`（按 provider/model 价格表）；`webapp/app_fastapi.py::api_ai_write_log` | 完成一次 AI 生成后，卡片底部自动出现 Token 和费用信息；或者「⚙️ AI 配置」看历史调用统计 |
| 题目合理性（生成的题目符合知识点/难度/约束） | 2 分 | 生成流程 prompt 中强制要求"先列知识点 → 题面 → 输入输出描述 → 样例 → 5 个隐藏测试点"；失败时 Mock fallback（避免全空） | `🤖_AI命题.py` 草稿区左侧展示题面结构 | `webapp/ai_engine.py::PROMPT_TEMPLATE`（结构化） | 实际展示一道 AI 产出的题目，看是否能通过正常判题（即测试点合法、样例输入 → 样例输出可通过） |
| 测试用例有效性（覆盖边界条件、区分时间复杂度） | 2 分 | `commit_draft` 时会用样例执行一次「自测判题」：若 sample 通过则自动写样例为第一条测试点；隐藏测试点按 prompt 要求生成（N 小、N 大、全 0、负数等） | `🤖_AI命题.py` 草稿「测试点预览」Tab | `webapp/app_fastapi.py::api_ai_commit_draft` 内部 `_validate_cases_against_sample` | 打开一份 AI 草稿看测试点：至少包含样例、边界（空输入/全 0）、大规模随机 |
| 功能易用性（流程顺畅、结果展示清晰） | 2 分 | 全流程：AI 配置 → AI 命题（需求 → 进度 → 草稿）→ 一键入库 → 题库出现新题 → 判题器可正常提交 | 四个页面无缝跳转 | `webapp/pages/⚙️_AI配置.py` → `🤖_AI命题.py` → `🗂_题目管理.py` → `⚖️_判题器.py` 完整链路 | 从头到尾演示一遍「配置模型 → AI 出一道题 → 入库 → 自己写代码 A 掉」 |
| Advance **合计** | 10 分 | — | — | — | — |

---

## 扣分项（对应 `requirements/` 页）

| 官方扣分项 | 本项目采取的规避手段 | 对应源码 / 配置 |
| --- | --- | --- |
| **大文件提交**（数据库、二进制、.zip 等超过 10MB 会扣分） | `.gitignore` 明确屏蔽 `*.db / *.log / __pycache__/ / .venv/ / dist/ / *.exe`；项目根目录没放任何大于 5MB 的文件 | [.gitignore](file:///c:/Users/m1994/Desktop/大作业2-OJ调试平台/.gitignore) 顶部注释 + 三条 pattern 说明 |
| **非 Conventional Commits**（不按 feat/fix/docs 规范扣分） | 本轮改动都按 Conventional Commits 提交：`docs: add per-file module docstrings for defense`、`fix: profile metric cleanup and page_url('problems') routing` 等 | 用 `git log --oneline -n 20` 查看 |
| 未使用异步接口 / 前端直连 DB（直接 0 分） | 见 Step1~6 总览前置硬约束 | `webapp/app_fastapi.py` 所有路由均 `async def`；前端 pages/ 搜索 `sqlite3` 或 `userdb import` → 0 结果 |

---

## 附录 A：快速演示脚本（答辩照着点就行）

```text
Step 1（题目管理）：
  1. admin 登录 → 左侧 🗂 题目管理 → 翻页 / 难度筛选
  2. 管理员 Tab → 新增 A+B（title: A+B Problem, difficulty: easy）
  3. 新题出现 → 点「详情」→ 编辑改标题 → 删除（记得先恢复回来）

Step 2（评测 + 动态语言注册）：
  1. 🔣 语言管理 → 新增 Go → 应用
  2. 回 🗂 题目管理 → 打开 A+B → 点「去判题」
  3. Python 写 AC 代码提交；再写死循环提交看 TLE

Step 3（提交 + 重判）：
  1. 📋 提交日志 → 按 WA 筛选 → 点某条「重判」
  2. 点某条「详情」进入 🧾 提交详情

Step 4（用户 + 权限）：
  1. 🔐 登录注册 → 注册 testuser1 → 自动跳 👤 个人信息（看 metric）
  2. 切回 admin → 👥 用户管理 → 把 testuser1 改成 banned
  3. 切回 testuser1 → 刷新 → 自动被踢

Step 5（日志权限 + 审计）：
  1. admin 在 A+B 的 📄 题目详情 → 「可见性」Tab → 打勾 public_cases
  2. 用普通用户打开 A+B 的 🧾 提交详情 → 测试点能展开
  3. admin 看底部「访问审计」表格

Step 6（前端链路）：
  1. 从 👤 个人信息点「📚 开始做题（进入题库）」→ 成功跳 🗂 题目管理（v1.4.1 修复了原来跳 ⚖️ 判题器的死链）
  2. 故意关 FastAPI → 任何页面请求都出现 common.api() 的红色友好错误，而非白屏

Advance AI（有网时演示，无网直接讲架构）：
  1. ⚙️ AI 配置 → 填 DeepSeek + 测试连通
  2. 🤖 AI 命题 → 知识点「链表反转」、难度 Medium → 开始生成
  3. 中途点中断；或等它生成完 → 草稿 → 一键入库 → 题库里出现新题
```

---

## 附录 B：版本信息 & 交付入口

| 项 | 值 |
| --- | --- |
| 版本号 | `v1.4.1`（三处标注：`common.OJ_VERSION`、`app_fastapi._OJ_VERSION`、`app_streamlit` 首页标题） |
| 启动入口（Windows） | [run_oj.bat](file:///c:/Users/m1994/Desktop/大作业2-OJ调试平台/run_oj.bat)（双击；或 cmd 中运行 `cd /d {proj} & run_oj.bat`） |
| 启动入口（WSL / Linux） | 管理员双击 [setup_linux.cmd](file:///c:/Users/m1994/Desktop/大作业2-OJ调试平台/setup_linux.cmd) → 完成后 WSL 中 `~/oj_p2/run_oj_linux.sh` |
| 默认 admin 账号 | `admin / admintestpassword`（首次启动自动种子，不可重复注册） |
| 默认 URL | Streamlit: `http://127.0.0.1:8501/`；FastAPI Swagger: `http://127.0.0.1:5000/docs` |
| 数据库位置 | `webapp/oj_users.db`（用户+sessions+access_logs）、`webapp/oj.db`（problems/submissions/languages/classes/...）、AI 表同 `oj.db` 的 `ai_config/ai_task_logs/ai_drafts/ai_reviews` |
